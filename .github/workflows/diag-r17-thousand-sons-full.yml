name: Diagnose R17 Thousand Sons full
on:
  push:
    paths:
      - '.github/build/diag_r17_thousand_sons_full.py'
      - '.github/workflows/diag-r17-thousand-sons-full.yml'
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Run Thousand Sons diagnostic
        run: python .github/build/diag_r17_thousand_sons_full.py
      - name: Commit diagnostic
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add inspection-r17-thousand-sons-full.txt
          git diff --cached --quiet || git commit -m "Diagnose R17 Thousand Sons catalogue"
          git push
