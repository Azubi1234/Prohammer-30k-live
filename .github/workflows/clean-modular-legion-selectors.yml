name: Publish structural Legion identity and TS Terminators

on:
  push:
    branches: [main]
    paths:
      - '.github/build/structural_legion_identity.py'
      - '.github/build/live_r70_ts_veteran_terminator_brotherhood.py'
      - '.github/workflows/clean-modular-legion-selectors.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Make Legion identity structural
        run: python .github/build/structural_legion_identity.py

      - name: Repair TS Veteran and Terminator Brotherhood wiring
        run: python .github/build/live_r70_ts_veteran_terminator_brotherhood.py

      - name: Synchronize staged mirrors
        run: |
          cp Legiones-Astartes-Generic.cat modular-catalogues-generated/Legiones-Astartes-Generic.cat
          cp index.xml index-modular-staging.xml

      - name: Final XML validation
        run: |
          python - <<'PY'
          import xml.etree.ElementTree as ET
          NS="http://www.battlescribe.net/schema/catalogueSchema"; C=lambda t:f"{{{NS}}}{t}"
          legion_files=["Dark-Angels.cat","Emperor-s-Children.cat","Iron-Warriors.cat","White-Scars.cat","Space-Wolves.cat","Imperial-Fists.cat","Night-Lords.cat","Blood-Angels.cat","Iron-Hands.cat","World-Eaters.cat","Ultramarines.cat","Death-Guard.cat","Thousand-Sons.cat","Sons-of-Horus.cat","Word-Bearers.cat","Salamanders.cat","Raven-Guard.cat","Alpha-Legion.cat"]
          g=ET.parse("Legiones-Astartes-Generic.cat").getroot()
          assert not any(e.get("id")=="config-army" for e in g.findall(f"./{C('selectionEntries')}/{C('selectionEntry')}"))
          for fn in legion_files:
              r=ET.parse(fn).getroot()
              cfg=[e for e in r.findall(f"./{C('selectionEntries')}/{C('selectionEntry')}") if e.get("id")=="config-army"]
              assert len(cfg)==1,fn
              lg=next(x for x in cfg[0].iter(C("selectionEntryGroup")) if x.get("id")=="config-legion")
              assert len(lg.findall(f"./{C('selectionEntries')}/{C('selectionEntry')}"))==1,fn
          ids={x.get("id"):x for x in g.iter() if x.get("id")}
          tcg=ids["r45-cult-terminator-unit"]
          term=ids["terminator-unit"]
          direct=term.findall(f"./{C('selectionEntryGroups')}/{C('selectionEntryGroup')}")
          assert not any("Psychic Brotherhood" in (x.get("name") or "") for x in direct), "stale sibling Terminator psychic selector remains"
          for cult in tcg.findall(f"./{C('selectionEntries')}/{C('selectionEntry')}"):
              pgs=[x for x in cult.findall(f"./{C('selectionEntryGroups')}/{C('selectionEntryGroup')}") if "Psychic Brotherhood Power" in (x.get("name") or "")]
              assert len(pgs)==1,cult.get("name")
              pg=pgs[0]
              assert pg.get("hidden")=="true",cult.get("name")
              cs=pg.findall(f"./{C('constraints')}/{C('constraint')}")
              assert any(c.get("type")=="min" and c.get("value")=="0" for c in cs),cult.get("name")
              assert any(c.get("type")=="max" and c.get("value")=="1" for c in cs),cult.get("name")
              mods=pg.findall(f"./{C('modifiers')}/{C('modifier')}")
              assert any(m.get("field")=="hidden" and m.get("value")=="false" for m in mods),cult.get("name")
              assert any(m.get("value")=="1" and (m.get("field") or "").endswith("-min") for m in mods),cult.get("name")
              powers=pg.findall(f"./{C('entryLinks')}/{C('entryLink')}")
              assert len(powers)==7,cult.get("name")
              for power in powers:
                  pc=next(c for c in power.findall(f"./{C('constraints')}/{C('constraint')}") if c.get("type")=="max")
                  assert pc.get("value")=="1",(cult.get("name"),power.get("name"))
                  assert not power.findall(f"./{C('modifiers')}/{C('modifier')}"),(cult.get("name"),power.get("name"))
          ET.parse("index.xml")
          ET.parse("index-modular-staging.xml")
          print("PASS: fixed local Legion identity; Allegiance/Rites retained; TS Terminator powers use Veteran show/min gating after Brotherhood; staging index mirrors live")
          PY

      - name: Commit live catalogue
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Legiones-Astartes-Generic.cat modular-catalogues-generated/Legiones-Astartes-Generic.cat *.cat index.xml index-modular-staging.xml inspection-ts-veteran-terminator-brotherhood.txt
          git diff --cached --quiet || git commit -m "Make Legion identity structural and fix TS Terminator powers"
          git push
