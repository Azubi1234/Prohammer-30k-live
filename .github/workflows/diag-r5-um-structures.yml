name: Diagnose R5 Ultramarines structures
on:
  push:
    paths:
      - '.github/build/diag_r5_um_structures.py'
      - '.github/workflows/diag-r5-um-structures.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/build/diag_r5_um_structures.py
      - name: Commit diagnostic
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add inspection-r5-um-structures.txt
          git diff --cached --quiet || git commit -m 'Diagnostic: R5 Ultramarines structures'
          git pull --rebase origin main
          git push
