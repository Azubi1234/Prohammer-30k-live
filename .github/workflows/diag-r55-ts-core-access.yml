name: Diagnose R55 TS Core Access
on:
  push:
    paths: ['.github/workflows/diag-r55-ts-core-access.yml']
permissions: {contents: read}
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.x'}
      - run: python .github/build/diag_r55_ts_core_access.py
