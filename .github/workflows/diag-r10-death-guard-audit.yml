name: Diagnose R10 Death Guard
on:
  push:
    paths:
      - '.github/build/diag_r10_death_guard_audit.py'
      - '.github/workflows/diag-r10-death-guard-audit.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/build/diag_r10_death_guard_audit.py
      - name: Commit diagnostic
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add inspection-r10-death-guard-audit.txt
          git diff --cached --quiet || git commit -m 'Diagnostic: R10 Death Guard audit'
          git pull --rebase origin main
          git push
